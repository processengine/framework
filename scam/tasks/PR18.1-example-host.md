# Task Contract: PR18.1 — Standalone domain host-service example

## Outcome
A minimal, standalone example service that consumes only the published packages
and public APIs, giving external teams a copyable starting template.

## Scope
- In: `examples/host-service/` (own package.json depending on published
  `@processengine/*`, a tiny flow, one operation, tests, README).
- Out: reusing test-shop internals.

## Affected module
`examples/`.

## Acceptance — frozen
- [ ] The example depends only on published `@processengine/*` versions (no local
      path/tarball imports) and builds clean.
- [ ] It runs one process to a terminal state against a local Kafka+PG.
- [ ] Its README is sufficient to run it with no other repo context.

## Required tests
Example build + a single end-to-end smoke test.

## Dependencies
PR1.1, published packages. **Priority** P1 · **Size** M · **Blocks stable** no.

## Docs
`examples/host-service/README.md`.
