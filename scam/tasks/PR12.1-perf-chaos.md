# Task Contract: PR12.1 — Load, soak & chaos harness with baselines

## Outcome
A repeatable harness produces published performance baselines and exercises long
soak + chaos, with regression gating.

## Scope
- In: `perf/` harness (load generator, soak, chaos hooks reusing resilience
  primitives); `docs/perf/BASELINES.md`.
- Out: production capacity guarantees.

## Affected module
Cross-cutting; runs against the contour.

## Acceptance — frozen
- [ ] Recorded baselines: sustained checkout throughput, p50/p99 completion
      latency, outbox depth under load.
- [ ] A ≥1h soak run shows no unbounded memory/outbox growth.
- [ ] Broker/DB chaos during load keeps logical exactly-once effects.
- [ ] Baselines are stored and a CI (nightly) job flags regressions.

## Required tests
The harness itself; nightly regression job.

## Dependencies
PR7.1 (HA target), PR10.1 (metrics). **Priority** P1 · **Size** L · **Blocks stable** no.

## Docs
`docs/perf/BASELINES.md`.
