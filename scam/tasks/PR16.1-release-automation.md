# Task Contract: PR16.1 — Release automation & npm provenance

## Outcome
A tagged commit reproducibly builds, verifies, and publishes all three packages
in dependency order with provenance, plus post-publish clean-install verification.

## Scope
- In: `.github/workflows/release.yml`, changelog generation, ordered publish;
  `docs/dev/RELEASE.md`.
- Out: the SBOM/scanning content (PR14.1) beyond wiring it in.

## Affected module
Repo release pipeline; all three packages.

## Acceptance — frozen
- [ ] Pushing tag `vX.Y.Z` builds + checks, then publishes conductor →
      transport-kafka → storage-postgres with `--provenance` and `--access public`.
- [ ] Post-publish job installs the published versions into a clean project and
      runs a public-import smoke — failing the release if it breaks.
- [ ] Version consistency across packages is validated before publish.

## Required tests
Dry-run publish in CI; post-publish clean-install smoke.

## Dependencies
PR14.1, PR15.1. **Priority** P1 · **Size** M · **Blocks stable** no.

## Docs
`docs/dev/RELEASE.md`.
