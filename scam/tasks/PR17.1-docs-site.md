# Task Contract: PR17.1 — API reference, tutorial & production deployment guide

## Outcome
A documentation set: generated API reference, a "first process in 20 minutes"
tutorial, and a production deployment guide cross-linking HA/security/observability.

## Scope
- In: `docs/` (generated API ref from PR1.1, tutorial, prod deploy guide).
- Out: hosting/site infra choices beyond static output.

## Affected module
Docs; sources from all packages.

## Acceptance — frozen
- [ ] Generated API reference covers the public surface (PR1.1) and builds in CI.
- [ ] Tutorial: a reader builds and runs one process end-to-end against local
      Kafka+PG following only the doc.
- [ ] Production deployment guide references PR7.1/PR8.1/PR9.1/PR10.1.

## Required tests
Doc build + link check in CI; tutorial validated by running its commands.

## Dependencies
PR1.1. **Priority** P1 · **Size** L · **Blocks stable** no.

## Docs
`docs/` site.
